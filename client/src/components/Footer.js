const Footer = () => (
  // add padding to the left and set a border above
  // span 2 columns (this means we consider the two columns as one and our content spans across it)
  <div>
    A project by <a href="https://github.com/bilala45">Bilal Ali</a>
  </div>
);
export default Footer;
